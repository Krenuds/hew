# Rust
/target/
shells/tauri/src-tauri/target/
shells/tauri/src-tauri/gen/schemas/

# JS
node_modules/
dist/

# wasm-pack output (generated by scripts/verify.sh / wasm build)
app/src/wasm/pkg/

# hew-cli sidecar binary staged for the Tauri bundle (generated by
# shells/tauri/scripts/stage-cli-sidecar.mjs)
shells/tauri/src-tauri/binaries/

# Playwright — installed browsers, reports, traces
app/test-results/
app/playwright-report/
app/blob-report/
app/playwright/.cache/

# Editor/OS noise
.DS_Store
*.swp
crates/skp-import/target/

# Internal design documents — never published
docs/design/

# Packaging signing key material — never committed, held only as a CI secret
# and the maintainer's own offline backup
hew-packaging-*.asc

# Scratch space render-templates.sh leaves the downloaded .deb files in
packaging/render-cache/

# wrangler local dev state / secrets (workers/share-relay)
.wrangler/
.dev.vars
site/public/videos/
