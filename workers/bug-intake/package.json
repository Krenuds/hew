{
  "name": "bug-intake",
  "private": true,
  "version": "0.0.0",
  "description": "Cloudflare Worker: private intake service behind Hew's Report Bug dialog.",
  "type": "module",
  "scripts": {
    "test": "node --test src/*.test.ts",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
